<div class="Page-Area">
  <%= link_to(equipment_path) do %>
    <div class="Back-Link-Area">
      <div class="Back-Link-Icon">
        <span class="material-icons-outlined">
        undo
        </span>
      </div>
      <div class="Back-Link-Text">
      備品管理へ戻る
      </div>
    </div>
  <% end %>
    <div class="Page-Title">
    管理備品一覧（子棚）
    </div>
    <div class="Page-Text">
    管理備品（子棚）を一覧で確認できます
    </div>
    <div class="DashBoard">
      <table class="table">
        <tr>
            <td>品目ID</td>
            <td>親棚番号</td>
            <td>子棚番号</td>
            <td>品目名</td>
            <td>寸法</td>            
            <td>備考</td>
            <td>カテゴリ</td>
            <td>画像パス</td>
            <td>最終更新者</td>
            <td>登録日時</td>
            <td>更新日時</td>
            <td></td>
        </tr>
        <% @equipmentItems.each do |item| %>
          <tr>
            <div class="list-item">
            <td><%= link_to(format("%04d", item.equipment_id.presence || '---'), "#",class:"list-link") %></td>
            <td><%= link_to(format("%04d", item.shelf_id.presence || '---'), "#",class:"list-link") %></td>
            <td><%= link_to(format("%04d", item.id.presence || '---'), "#",class:"list-link") %></td>
            <td><%= item.name.presence || '---' %></td>
            <td><%= formatSize(item.size).presence || '---' %></td>
            <td><%= item.remark.presence || '---' %></td>
            <td><%= item.category.presence || '---' %></td>
            <td><%= item.image.presence || '---' %></td>
            <td><%= getProfileByUserID(item.user_id).username.presence || '---' %></td>
            <td><%= item.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            <td><%= item.updated_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            <td><%= link_to("詳細", "#",class:"btn btn-outline-primary") %></td>
            </div>
          </tr>
        <% end %>
      </table>
    </div>
</div>