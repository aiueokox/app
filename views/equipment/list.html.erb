<div class="Page-Area">
  <%= link_to(equipment_path) do %>
    <div class="Back-Link-Area">
      <div class="Back-Link-Icon">
        <span class="material-icons-outlined">
        undo
        </span>
      </div>
      <div class="Back-Link-Text">
      備品管理へ戻る
      </div>
    </div>
  <% end %>
    <div class="Page-Title">
    管理備品一覧（品目）
    </div>
    <div class="Page-Text">
    管理備品（品目）を一覧で確認できます
    </div>
    <div class="DashBoard">
      <table class="table">
        <tr>
            <td>ID</td>
            <td>品目名</td>
            <td>寸法</td>            
            <td>備考</td>
            <td>カテゴリ</td>
            <td>画像パス</td>
            <td>最終更新者</td>
            <td>登録日時</td>
            <td>更新日時</td>
            <td></td>
        </tr>
        <% @equipment.each do |equipment| %>
          <tr>
            <div class="list-item">
            <td><%= link_to(format("%04d", equipment.id.presence || '---'), "/equipment/info/#{equipment.id}",class:"list-link") %></td>
            <td><%= equipment.name.presence || '---' %></td>
            <td><%= formatSize(equipment.size).presence || '---' %></td>
            <td><%= equipment.remark.presence || '---' %></td>
            <td><%= equipment.category.presence || '---' %></td>
            <td><%= equipment.image.presence || '---' %></td>
            <td><%= getProfileByUserID(equipment.user_id).username.presence || '---' %></td>
            <td><%= equipment.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            <td><%= equipment.updated_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            <td><%= link_to("詳細", "/equipment/info/#{equipment.id}",class:"btn btn-outline-primary") %></td>
            </div>
          </tr>
        <% end %>
      </table>
    </div>
</div>