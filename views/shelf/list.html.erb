<div class="Page-Area">
  <%= link_to(equipment_path) do %>
    <div class="Back-Link-Area">
      <div class="Back-Link-Icon">
        <span class="material-icons-outlined">
        undo
        </span>
      </div>
      <div class="Back-Link-Text">
      備品管理へ戻る
      </div>
    </div>
  <% end %>
    <div class="Page-Title">
    親棚一覧
    </div>
    <div class="Page-Text">
    親棚を一覧で確認できます
    </div>
    <div class="DashBoard">
      <table class="table">
        <tr>
            <td>ID</td>
            <td>部屋番号</td>
            <td>備考</td>
            <td>最終更新者</td>
            <td>登録日時</td>
            <td>更新日時</td>
        </tr>
        <% @shelves.each do |shelf| %>
          <tr>
            <div class="list-item">
            <td><%= link_to(format("%04d", shelf.id.presence || '---'), "#",class:"list-link") %></td>
            <td><%= shelf.room.presence || '---' %></td>
            <td><%= shelf.remark.presence || '---' %></td>
            <td><%= getProfileByUserID(shelf.user_id).username.presence || '---' %></td>
            <td><%= shelf.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            <td><%= shelf.updated_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
            </div>
          </tr>
        <% end %>
      </table>
    </div>
</div>